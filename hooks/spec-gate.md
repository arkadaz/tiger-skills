---
event: UserPromptSubmit
---

SPEC GATE: If this request is a BUILD request — add / build / implement / create a feature, "I want…", "can we…", or any change to user-visible behavior — and no HUMAN-approved spec covers it, invoke harness-engineering-grill FIRST. Interview the user, write the spec, get human approval, then plan. Do NOT plan or write code without an approved spec. "Approved" means the human said yes to the spec in the conversation — never approve a spec yourself. A spec file you authored or edited this session is a draft, not approval; a spec the user pasted is grill INPUT, not approval. Do NOT write Status: approved yourself, and do NOT let bootstrap pre-create or pre-approve specs to skip this gate. Skip this gate only for bug fixes, factual questions, and precise one-line edits.
